# ClipTune

**The Speed of AI. The Precision of a Pro.**

ClipTune is an AI-powered video clipper that gives creators full control over the editing process via a timeline interface.

## Tech Stack
- **Framework:** Next.js 14 (App Router)
- **Styling:** Tailwind CSS
- **Language:** TypeScript

## Getting Started

1.  Install dependencies:
    ```bash
    npm install
    ```

2.  Run the development server:
    ```bash
    npm run dev
    ```

3.  Open [http://localhost:3000](http://localhost:3000) with your browser.

## Project Structure
- `/src/app`: App Router pages and layouts.
- `/src/components`: Reusable UI components.
- `/src/lib`: Utility functions and API clients.

## Roadmap
See `mvp_spec.md` for the detailed feature roadmap.
